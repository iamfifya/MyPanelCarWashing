<Window x:Class="MyPanelCarWashing.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPanelCarWashing"
        mc:Ignorable="d"
        Title="Панель управления автомойкой"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinWidth="800" MinHeight="600"
        Width="1024" Height="768"
        FontFamily="Segoe UI"
        WindowState="Maximized">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с кнопками и фильтрами -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10" Margin="0,0,0,10" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="➕ Добавить заказ" x:Name="AddButton" Click="AddButton_Click" Width="150" Margin="5" Background="#3498DB"/>
                <Button Content="👥 Сотрудники" x:Name="EmployeesButton" Click="EmployeesButton_Click" Width="120" Margin="5" Background="#2C3E50"/>
                <Separator Width="20" Visibility="Collapsed"/>
                <Label Content="🔍 Поиск:" VerticalContentAlignment="Center" Margin="5,0,0,0"/>
                <TextBox Width="200" x:Name="SearchFilterTextBox" KeyUp="SearchFilterTextBox_KeyUp" 
                         VerticalContentAlignment="Center" Margin="5" Padding="5"/>
            </StackPanel>
        </Border>

        <!-- Список заказов -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding OrdersList}"
                  x:Name="OrdersListView"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  Cursor="Hand"
                  Margin="0,0,0,10">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Background" Value="White"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F0F8FF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="✏ Редактировать услуги" x:Name="EditItem" Click="EditItem_Click"/>
                    <MenuItem Header="🗑 Удалить" x:Name="DeleteItem" Click="DeleteItem_Click"/>
                </ContextMenu>
            </ListView.ContextMenu>

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="#BDC3C7" CornerRadius="8" 
                            Background="White" Width="280" Height="200" Margin="5">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.2"/>
                        </Border.Effect>
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" FontSize="18" FontWeight="Bold" 
                                       Text="{Binding CarModel}" HorizontalAlignment="Center"
                                       Foreground="#2C3E50"/>
                            <TextBlock Grid.Row="1" FontSize="14" 
                                       Text="{Binding CarNumber}" HorizontalAlignment="Center"
                                       Margin="0,5,0,0" Foreground="#7F8C8D"/>
                            <TextBlock Grid.Row="2" FontSize="14" 
                                       Text="{Binding Time, StringFormat=Время: {0:HH:mm}}" 
                                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="3" FontSize="16" FontWeight="Bold"
                                       Text="{Binding TotalPrice, StringFormat=Сумма: {0:C}}" 
                                       HorizontalAlignment="Center" Margin="0,10,0,0"
                                       Foreground="#27AE60"/>
                            <TextBlock Grid.Row="4" FontSize="11"
                                       Text="{Binding ServicesCount}" 
                                       HorizontalAlignment="Center" Margin="0,5,0,0"
                                       Foreground="#3498DB"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Нижняя панель с навигацией -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="10" CornerRadius="5">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="ExitButton" Content="🚪 Выход" Click="ExitButton_Click" 
                            Width="100" Margin="5" Background="#E74C3C"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="PrevPage" Content="◀ Назад" Click="PrevPage_Click" 
                            Width="80" Margin="5" Background="#95A5A6"/>
                    <TextBlock Text="{Binding CurrentPage, StringFormat=Страница {0}}" 
                               VerticalAlignment="Center" Margin="5" FontSize="12" FontWeight="Bold"/>
                    <Button x:Name="NextPage" Content="Вперед ▶" Click="NextPage_Click" 
                            Width="80" Margin="5" Background="#95A5A6"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>