<Window x:Class="MyPanelCarWashing.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPanelCarWashing"
        mc:Ignorable="d"
        Title="Авторизация"
        WindowStartupLocation="CenterScreen"
        Height="550" Width="400"
        Background="#F5F6FA"
        BorderBrush="Black"
        BorderThickness="1"
        WindowStyle="None">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Глянец" FontSize="72" 
                   Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,20" Grid.Row="0" FontFamily="Segoe Print" FontWeight="Bold"/>

        <Label Content="Логин:" Grid.Row="1" Margin="0,5" FontWeight="SemiBold"/>
        <TextBox x:Name="LoginTextBox" Grid.Row="2" Margin="0,5" Height="50" Padding="5" Foreground="Black"/>

        <Label Content="Пароль:" Grid.Row="3" Margin="0,5" FontWeight="SemiBold"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Margin="0,5" Height="50" Padding="5" VerticalContentAlignment="Center" Foreground="Black"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button Content="Войти" Click="LoginButton_Click" Width="100" Height="35" 
                    Margin="5" Background="#27AE60" Foreground="White" Cursor="Hand"/>
            <Button Content="Выход" Click="ExitButton_Click" Width="100" Height="35" 
                    Margin="5" Background="#E74C3C" Foreground="White" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
