<Window x:Class="MyPanelCarWashing.ServiceManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPanelCarWashing"
        mc:Ignorable="d"
        Title="Управление услугами"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinWidth="800" MinHeight="500"
        Width="900" Height="600"
        Background="#F5F6FA" WindowState="Maximized">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🛠️ Управление услугами" FontSize="24" FontWeight="Bold" 
                   Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- Кнопки управления и поиск -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Left">
            <Button Content="➕ Добавить услугу" Click="AddButton_Click" Width="140" Height="35" 
                    Margin="5" Background="#27AE60" Foreground="White"/>
            <Button Content="✏ Редактировать" Click="EditButton_Click" Width="120" Height="35" 
                    Margin="5" Background="#3498DB" Foreground="White"/>
            <Button Content="🗑 Удалить" Click="DeleteButton_Click" Width="100" Height="35" 
                    Margin="5" Background="#E74C3C" Foreground="White"/>
            <Button Content="🔄 Обновить" Click="RefreshButton_Click" Width="100" Height="35" 
                    Margin="5" Background="#95A5A6" Foreground="White"/>
            <Label Content="🔍 Поиск:" VerticalContentAlignment="Center" Margin="20,0,0,0"/>
            <TextBox x:Name="SearchTextBox" Width="200" Height="30" Margin="5" Padding="5"
                     TextChanged="SearchTextBox_TextChanged"/>
        </StackPanel>
        <!-- Цены по категориям -->
        <Label Content="Цены по категориям кузова:" Grid.Row="2" Margin="0,5" FontWeight="SemiBold"/>

        <Grid Grid.Row="3" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="5">
                <Label Content="Категория 1 (Седан)" FontSize="10"/>
                <TextBox Height="40" x:Name="PriceCategory1TextBox" Text="0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="5">
                <Label Content="Категория 2 (Хэтчбек)" FontSize="10"/>
                <TextBox Height="40" x:Name="PriceCategory2TextBox" Text="0" />
            </StackPanel>

            <StackPanel Grid.Column="2" Margin="5">
                <Label Content="Категория 3 (Универсал)" FontSize="10"/>
                <TextBox Height="40" x:Name="PriceCategory3TextBox" Text="0" />
            </StackPanel>

            <StackPanel Grid.Column="3" Margin="5">
                <Label Content="Категория 4 (Внедорожник)" FontSize="10"/>
                <TextBox Height="40" x:Name="PriceCategory4TextBox" Text="0" />
            </StackPanel>
        </Grid>

        <!-- Список услуг в виде карточек -->
        <ListView Grid.Row="4"
                  x:Name="ServicesListView"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  Cursor="Hand"
                  Margin="0,0,0,10">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Background" Value="White"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F0F8FF"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E3F2FD"/>
                            <Setter Property="BorderBrush" Value="#2196F3"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="✏ Редактировать" Click="EditButton_Click"/>
                    <MenuItem Header="🗑 Удалить" Click="DeleteButton_Click"/>
                </ContextMenu>
            </ListView.ContextMenu>

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="#BDC3C7" CornerRadius="8" 
                            Background="White" Width="280" Height="180" Margin="5">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.2"/>
                        </Border.Effect>
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" FontSize="16" FontWeight="Bold" 
                                       Text="{Binding Name}" HorizontalAlignment="Center"
                                       Foreground="#2C3E50" TextWrapping="Wrap" TextAlignment="Center"/>

                            <TextBlock Grid.Row="1" FontSize="14" FontWeight="Bold"
                                       Text="{Binding Price, StringFormat=💰 {0:N0} ₽}" 
                                       HorizontalAlignment="Center" Margin="0,10,0,0"
                                       Foreground="#27AE60"/>

                            <TextBlock Grid.Row="2" FontSize="11" 
                                       Text="{Binding DurationMinutes, StringFormat=⏱️ Длительность: {0} мин}" 
                                       HorizontalAlignment="Center" Margin="0,5,0,0"
                                       Foreground="#7F8C8D"/>

                            <TextBlock Grid.Row="3" FontSize="11" 
                                       Text="{Binding Description}" 
                                       HorizontalAlignment="Center" Margin="0,5,0,0"
                                       Foreground="#95A5A6" TextWrapping="Wrap" TextAlignment="Center"
                                       MaxHeight="50"/>

                            <Border Grid.Row="4" CornerRadius="10" Padding="5,2" Margin="0,8,0,0"
                                    Background="{Binding IsActive, Converter={StaticResource ActiveToBackgroundConverter}}"
                                    HorizontalAlignment="Center">
                                <TextBlock FontSize="10" Foreground="White"
                                           Text="{Binding IsActive, Converter={StaticResource ActiveToStatusConverter}}"/>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Кнопка закрытия -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="🔧 Проверить" Click="CheckIntegrityButton_Click" Width="130" Height="35" 
                    Margin="5" Background="#F39C12" Foreground="White"/>
            <Button Content="✖ Закрыть" Click="CloseButton_Click" Width="100" Height="35" 
                    Margin="5" Background="#E74C3C" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
