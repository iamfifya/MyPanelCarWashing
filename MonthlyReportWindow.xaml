<Window x:Class="MyPanelCarWashing.MonthlyReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPanelCarWashing"
        mc:Ignorable="d"
        Title="Месячный отчет"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinWidth="800" MinHeight="600"
        Width="900" Height="700"
        Background="#F5F6FA"
        BorderBrush="Black"
        BorderThickness="1"
        WindowStyle="None"
        WindowState="Maximized"
        Foreground="Black">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📊 Месячный отчет" FontSize="24" FontWeight="Bold" 
                   Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,15" Grid.Row="0"/>

        <!-- Выбор месяца -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Выберите месяц:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <local:CustomDatePicker x:Name="MonthPicker" Grid.Column="1" Height="50" 
                        SelectedDate="{Binding SelectedDate, Mode=TwoWay}"/>
            <Button Grid.Column="2" Content="📊 Сформировать отчет" Click="GenerateReportButton_Click" 
                    Width="180" Height="35" Margin="10,0,0,0"/>
        </Grid>

        <!-- Контент отчета -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Background="White">
            <StackPanel>
                <StackPanel x:Name="ReportContent" Visibility="Collapsed">
                    <!-- Итоговая статистика -->
                    <Border Background="#F8F9FA" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                <TextBlock Text="🚗 Всего машин" FontSize="12" Foreground="#7F8C8D"/>
                                <TextBlock x:Name="TotalCarsText" FontSize="24" FontWeight="Bold" 
                                           Foreground="#2C3E50" HorizontalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                <TextBlock Text="💰 Общая выручка" FontSize="12" Foreground="#7F8C8D"/>
                                <TextBlock x:Name="TotalRevenueText" FontSize="24" FontWeight="Bold" 
                                           Foreground="#27AE60" HorizontalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                <TextBlock Text="👤 Мойщикам (35%)" FontSize="12" Foreground="#7F8C8D"/>
                                <TextBlock x:Name="TotalWasherText" FontSize="24" FontWeight="Bold" 
                                           Foreground="#8E44AD" HorizontalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                                <TextBlock Text="🏢 Компании (65%)" FontSize="12" Foreground="#7F8C8D"/>
                                <TextBlock x:Name="TotalCompanyText" FontSize="24" FontWeight="Bold" 
                                           Foreground="#E67E22" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Grid.Column="4" HorizontalAlignment="Center">
                                <TextBlock Text="Уникальных клиентов:" Width="150" FontWeight="Bold" Foreground="#34495E"/>
                                <TextBlock x:Name="UniqueClientsText" HorizontalAlignment="Center" FontSize="24" Text="0" FontWeight="Bold" Foreground="#2980B9"/>
                            </StackPanel>

                            <StackPanel Grid.Column="5" HorizontalAlignment="Center">
                                <TextBlock Text="Новых клиентов:" Width="150" FontWeight="Bold" Foreground="#34495E"/>
                                <TextBlock x:Name="NewClientsText" HorizontalAlignment="Center" FontSize="24" Text="0" FontWeight="Bold" Foreground="#27AE60"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Статистика по способам оплаты -->
                    <Border Background="#F8F9FA" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="💳 Способы оплаты за месяц" FontSize="14" FontWeight="Bold" 
                   Foreground="#2C3E50" Margin="0,0,0,10"/>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Наличные -->
                                <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="10" Margin="5">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="💵 Наличные" FontSize="12" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                        <TextBlock x:Name="MonthlyCashCountText" FontSize="20" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="MonthlyCashAmountText" FontSize="12" Foreground="#7F8C8D"/>
                                    </StackPanel>
                                </Border>

                                <!-- Карта -->
                                <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="10" Margin="5">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="💳 Карта" FontSize="12" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                        <TextBlock x:Name="MonthlyCardCountText" FontSize="20" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="MonthlyCardAmountText" FontSize="12" Foreground="#7F8C8D"/>
                                    </StackPanel>
                                </Border>

                                <!-- Перевод -->
                                <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="10" Margin="5">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="📱 Перевод" FontSize="12" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                        <TextBlock x:Name="MonthlyTransferCountText" FontSize="20" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="MonthlyTransferAmountText" FontSize="12" Foreground="#7F8C8D"/>
                                    </StackPanel>
                                </Border>

                                <!-- QR-код -->
                                <Border Grid.Column="3" Background="White" CornerRadius="8" Padding="10" Margin="5">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="📱 QR-код" FontSize="12" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                        <TextBlock x:Name="MonthlyQrCountText" FontSize="20" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="MonthlyQrAmountText" FontSize="12" Foreground="#7F8C8D"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Таблица по дням -->
                    <TextBlock Text="📅 Отчет по дням" FontSize="16" FontWeight="Bold" 
                               Foreground="#2C3E50" Margin="0,0,0,10" Background="White"/>

                    <ListView x:Name="DailyReportsList" Height="250" Background="White">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Дата" Width="100" DisplayMemberBinding="{Binding Date, StringFormat=dd.MM.yyyy}"/>
                                <GridViewColumn Header="Машин" Width="80" DisplayMemberBinding="{Binding TotalCars}"/>
                                <GridViewColumn Header="Выручка" Width="120" DisplayMemberBinding="{Binding TotalRevenue, StringFormat={}{0:N0} ₽}"/>
                                <GridViewColumn Header="Мойщикам" Width="120" DisplayMemberBinding="{Binding TotalWasherEarnings, StringFormat={}{0:N0} ₽}"/>
                                <GridViewColumn Header="Компании" Width="120" DisplayMemberBinding="{Binding TotalCompanyEarnings, StringFormat={}{0:N0} ₽}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- Заработная плата сотрудников -->
                    <TextBlock Text="💰 Заработная плата сотрудников за месяц" FontSize="16" FontWeight="Bold" 
                               Foreground="#2C3E50" Margin="0,15,0,5"/>
                    <TextBlock Text="(35% от выручки по заказам, которые выполнил сотрудник)" 
                               FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,10"/>

                    <ListView x:Name="EmployeesSalaryList" Height="200" Background="White">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Сотрудник" Width="180" DisplayMemberBinding="{Binding EmployeeName}"/>
                                <GridViewColumn Header="Кол-во машин" Width="100" DisplayMemberBinding="{Binding CarsWashed}"/>
                                <GridViewColumn Header="Выручка сотрудника" Width="140" DisplayMemberBinding="{Binding TotalAmount, StringFormat={}{0:N0} ₽}"/>
                                <GridViewColumn Header="Заработная плата (35%)" Width="150" DisplayMemberBinding="{Binding Earnings, StringFormat={}{0:N0} ₽}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- Детальная статистика по сотрудникам за месяц -->
                    <TextBlock Text="👥 Детальная статистика сотрудников за месяц" FontSize="16" FontWeight="Bold" 
                               Foreground="#2C3E50" Margin="0,15,0,10"/>

                    <Expander Header="Развернуть детальную статистику по дням" Margin="0,0,0,10">
                        <ItemsControl x:Name="EmployeesDetailControl" Margin="0,5" Foreground="Black">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8F9FA" CornerRadius="8" Padding="10" Margin="0,5">
                                        <StackPanel>
                                            <TextBlock FontSize="14" FontWeight="Bold" Foreground="#2C3E50">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0} - {1:N0} ₽ (всего {2} машин)">
                                                        <Binding Path="EmployeeName"/>
                                                        <Binding Path="Earnings"/>
                                                        <Binding Path="CarsWashed"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <ItemsControl ItemsSource="{Binding DailyWork}" Margin="0,10,0,0">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Background="White" CornerRadius="5" Padding="8" Margin="0,2">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="100"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Text="{Binding Date, StringFormat=dd.MM.yyyy}" 
                                                                           FontWeight="SemiBold"/>
                                                                <TextBlock Grid.Column="1" Text="{Binding CarsWashed, StringFormat=🚗 {0} машин}" 
                                                                           Margin="10,0"/>
                                                                <TextBlock Grid.Column="2" Text="{Binding Earnings, StringFormat=💰 {0:N0} ₽}" 
                                                                           Foreground="#27AE60" FontWeight="Bold"/>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>
                </StackPanel>

                <TextBlock x:Name="NoDataText" Text="Выберите месяц и нажмите 'Сформировать отчет'" 
                           FontSize="14" Foreground="#7F8C8D" HorizontalAlignment="Center" 
                           Margin="0,50,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="📄 Экспорт" Click="ExportButton_Click" Width="100" Height="35" 
                    Margin="5" Background="#27AE60" Foreground="White"/>
            <Button Content="✖ Закрыть" Click="CloseButton_Click" Width="100" Height="35" 
                    Margin="5" Background="#E74C3C" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
