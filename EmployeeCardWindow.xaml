<Window x:Class="MyPanelCarWashing.EmployeeCardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPanelCarWashing"
        mc:Ignorable="d"
        Title="Сотрудники"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinWidth="600" MinHeight="400"
        Width="800" Height="500"
        Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10" Margin="0,0,0,10" CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ Добавить сотрудника" x:Name="AddButton" Click="AddButton_Click" 
                        Width="150" Margin="5" Background="#27AE60" Foreground="White"/>
                <Button Content="🔄 Обновить" x:Name="RefreshButton" Click="RefreshButton_Click" 
                        Width="100" Margin="5" Background="#3498DB" Foreground="White"/>
                <Separator Width="20" Visibility="Collapsed"/>
                <Label Content="🔍 Поиск:" VerticalContentAlignment="Center" Margin="5,0,0,0"/>
                <TextBox Width="200" x:Name="SearchFilterTextBox" KeyUp="SearchFilterTextBox_KeyUp" 
                         VerticalContentAlignment="Center" Margin="5" Padding="5"/>
            </StackPanel>
        </Border>

        <!-- Список сотрудников -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding EmployeesList}"
                  x:Name="EmployeesListView"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  Cursor="Hand">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Background" Value="White"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="✏ Редактировать" x:Name="EditItem" Click="EditItem_Click"/>
                    <MenuItem Header="🗑 Удалить" x:Name="DeleteItem" Click="DeleteItem_Click"/>
                </ContextMenu>
            </ListView.ContextMenu>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="ФИО" Width="200" DisplayMemberBinding="{Binding FullName}"/>
                    <GridViewColumn Header="Логин" Width="150" DisplayMemberBinding="{Binding Login}"/>
                    <GridViewColumn Header="Роль" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding IsAdmin, Converter={StaticResource BoolToRoleConverter}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="10" Margin="0,10,0,0" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CloseButton" Content="✖ Закрыть" Click="CloseButton_Click" 
                        Width="100" Margin="5" Background="#E74C3C" Foreground="White"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>